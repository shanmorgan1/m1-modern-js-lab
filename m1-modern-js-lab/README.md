Frontend development focuses on what users see and interact with in the browser. Backend development handles the server, databases, and processing data that happens on the back end.

The request response lifecycle starts when a user sends a request to a server. The server processes the request, gets the data, and sends a response back to the user.

Modular file organization is important because it keeps code manageable; allowing for easy editing and organization.
